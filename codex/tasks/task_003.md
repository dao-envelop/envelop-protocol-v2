# Remove Forge Warnings
## Проблема
`forge build` даёт много wornings. Мы  готовим минорный  релиз 2.2.1, поэтому нужн их убрать(устранить причину).
## Что делать.
1. `forge build` - посчитай  сколько  ворнингов. Сгруппируй их по типам. Покажи статистику. Все  файлы в папке ./src/mock можешь править самостоятельно (проверяй, что тесты не сломаются). Это отдельный коммит.  Потому  остальные,  если  сомнваешься -спроси пользователя. 
